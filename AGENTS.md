# Project Guidance

This repository is a prototype for a hackathon.

Do not use `claude` or `codex` as prefixes for branch names.

Search correctness and relevance behavior must be implemented in the shared
MCP/Elasticsearch layer so it applies to every consumer. Do not implement search
fixes only in the marketing site.
